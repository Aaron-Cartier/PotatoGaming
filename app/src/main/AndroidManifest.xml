<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.potatogaming">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".XboxActivity"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".Ps4Activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".GameInfo" />
        <activity
            android:name=".updateActivity"
            android:parentActivityName=".gameRequestListActivity" />
        <activity
            android:name=".RequestedGameInfo"
            android:parentActivityName=".gameRequestListActivity" />
        <activity
            android:name=".gameRequestListActivity"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".requestActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".localDatabase"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>